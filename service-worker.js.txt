self.addEventListener("install", event => {

event.waitUntil(

caches.open("fitness-cache").then(cache=>{
return cache.addAll([
"/",
"/index.html",
"/style.css",
"/app.js"
])
})

);

});